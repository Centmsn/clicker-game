import styled from "styled-components";

export const Title = styled.header`
  margin-bottom: 30px;
  padding-top: 30px;

  font-size: 1.5rem;
  color: ${({ theme }) => theme.colors.black};
  text-align: center;
`;
